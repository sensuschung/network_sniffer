# 基本信息
姓名: sensuschung
学号: 1234567890

# 项目构成
- `/src`:源代码文件夹，包括`filter.py`，`packet_processor.py`，`sniffer.py`，`storage.py`，`\img`，共5个文件/文件夹。开发环境为Windows/Linux，主要使用语言python（边执行边编译）。
- `/dist`:可执行文件文件夹，包括`sniffer.exe`。构建工具为pyinstaller，运行平台为Windows。
- `/test`/`test.py`:测试文件

- `requirements.txt`:项目依赖文件
